<%=
  render(
    partial: 'header',
    locals: {
      title: @featured.title,
      subtitle: "Week of #{DateTime.parse(@featured.published).strftime('%B %-d, %Y')}"
    }
  )
%>
<%=
  render(
    partial: 'post',
    locals: {
      featured: @featured,
      posts: unique_posts(@featured, @posts)
    }
  )
%>
<%= render partial: 'shared/newsletter' %>
<%= javascript_include_tag 'home' %>
